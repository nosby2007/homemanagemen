{
  "name": "functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "sendgrid:smoke": "node scripts/sendgrid-smoke.cjs"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.2.5",
    "graphql": "^16.12.0",
    "pdfkit": "^0.15.2"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.13.9",
    "typescript": "^5.1.6"
  }
}
